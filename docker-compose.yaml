services:
  mysql:
    image: 'mysql:8.0'
    environment:
      MYSQL_DATABASE: judgify
      MYSQL_USER: judgify
      MYSQL_PASSWORD: judgify
      MYSQL_ROOT_PASSWORD: root
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
